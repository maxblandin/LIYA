<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mgr.narratif.game.liya">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:screenOrientation="portrait"
        android:allowBackup="true"
        android:icon="@drawable/icon_liya"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vue.activity.MenuActivity">
        </activity>
        <activity android:name=".vue.activity.ChoixAventureActivity" />
        <activity android:name=".vue.activity.ChoixHerosActivity" />
        <activity android:name=".vue.activity.PeripetieActivity" />
        <activity android:name=".vue.activity.ApplicationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>